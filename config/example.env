# Network
NETWORK=phonchain-mainnet

# Server
# Gateway behind Nginx reverse-proxy (recommended):
HOST=127.0.0.1
PORT=5000

# Public endpoints (optional, for docs / bootstrap)
EXPLORER_URL=https://explorer.phonchain.org

# Bootstrap peers (example)
BOOTSTRAP_NODES=https://api.phonchain.org,https://seed.phonchain.org

# Internal protected endpoint (optional)
# NEVER commit a real secret. Use a long random value on your server only.
INTERNAL_TX_KEY=CHANGE_ME_LONG_RANDOM_SECRET

# Production flags
FLASK_ENV=production
